# Jachin Nexus V2 - L2 无状态节点镜像
# 从项目根构建，支持 core.main 入口
FROM docker.1ms.run/library/python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y gcc portaudio19-dev && rm -rf /var/lib/apt/lists/*

COPY core/requirements.txt ./core/
RUN pip install --no-cache-dir -r core/requirements.txt

COPY core/ ./core/
COPY static/ ./static/

ENV PYTHONPATH=/app

EXPOSE 18880

CMD ["uvicorn", "core.main:app", "--host", "0.0.0.0", "--port", "18880"]
