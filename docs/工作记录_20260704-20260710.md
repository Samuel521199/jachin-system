# Jachin 本周工作记录（2026-07-04 至 2026-07-10）

1. 推进 Jachin 主架构从旧 ReAct/兼容执行链路迁移到 Memory-first Cognitive Kernel 新架构。
2. 正式纳入并维护 `docs/07_memory_first_main_agent_and_voice_app_agents.md` 作为主架构设计文档。
3. 新增并维护 `docs/08_memory_first_cognitive_kernel_execution_plan.md`，明确新架构阶段化执行路线。
4. 搭建 ReviewBoard、Arbiter、DecisionContract、WorkOrder、RoleExecution、Verification、Recovery、TurnClosure 的主执行链路。
5. 将低风险 AppControl 任务从旧 Agent 推理路径迁移到 WorkOrder Dispatcher 直接执行。
6. 扩展 AppControl direct mainline，支持常用 App 的打开、切换、关闭、前台校验和失败反馈。
7. 增加常用 App 别名表，覆盖浏览器、微信、Lark、计算器、记事本、资源管理器等常用桌面应用。
8. 修复浏览器已启动但焦点验证失败的问题，优化后台启动后的窗口识别和校验逻辑。
9. 修复“关闭”指令误关上一个错误应用的问题，引入最近操作应用上下文和模糊关闭澄清机制。
10. 优化确认/取消任务流程，修复前端点击确认后后端找不到 pending task 的问题。
11. 将 File direct mainline 扩展到文件读取、打开、定位所在目录等安全操作。
12. 为文件写入、移动、删除等高风险操作保留确认门控，避免误执行危险动作。
13. 建立 Role Agent Registry，将 AppControl、File、Message、Verification、Recovery、MemoryWrite 等角色正式注册。
14. 建立 WorkOrder Dispatcher 权限矩阵，使工具调用由角色执行器承接，而不是由旧主循环直接调用。
15. 拆分 AppControlExecutorAgent 的执行策略，增加窗口状态、截图证据和前台 App 验证。
16. 拆分 FileExecutorAgent 的路径解析、安全预检、结果校验和失败原因记录。
17. 拆分 MessageExecutorAgent 的发送前预览、收件人解析、重复发送保护和发送后校验。
18. 增加 MessageExecutorAgent 的消息 hash 与 WorkOrder id 去重，降低重复发消息风险。
19. 接入 MemoryRecallAgent 多路检索骨架，支持从近期上下文、长期记忆、任务历史中补充执行依据。
20. 接入 MemoryWriteAgent 写入策略，支持任务结束后的摘要、证据和用户偏好沉淀。
21. 推进 Memory 生命周期设计，补充短期、长期、去重、合并、冲突标注等核心能力。
22. 建立 RecoveryPlanner，支持失败后基于错误原因生成下一步恢复方案。
23. 将 Recovery 候选路径升级为 Capability Metadata / Skill Manifest 驱动，降低主架构对具体业务策略的耦合。
24. 增强 Recovery 逻辑，使每次重试会吸收上一次失败原因，而不是一次性固定 B/C/D 路径。
25. 为 Skill/MCP 发布与安装链路补充 recovery_playbook schema 校验，提前发现错误恢复策略。
26. 打通 Cognitive Kernel ledger 与 OS Evidence Console 的统一证据查询思路。
27. 优化 Evidence Console 展示，让任务可按 ReviewBoard、Arbiter、WorkOrder、RoleExecution、Verification、Recovery、TurnClosure 阶段回放。
28. 清理旧架构文档、旧协议描述和旧 ReAct 入口，减少主流程对历史兼容代码的依赖。
29. 删除或重命名旧的 `react_*`、`critic_agent`、`pseudo_actions`、`react_ui_sanitize` 等遗留模块。
30. 将旧的 ActionCritic / SQLite 工具纠偏迁移为 WorkOrder Critic / Capability Hook 形式。
31. 优化 Capability Hook 机制，使业务守卫、工具入参校验、恢复策略从主 Agent 中剥离。
32. 推进 PMO、HR、SQLite、文件写回等业务策略向 Skill/Capability Hook 外挂化迁移。
33. 优化 L1 能力发布台，增强 Skill/MCP/Model 的发布、状态识别、版本迭代和校验能力。
34. 修复 L1 Profile 权限 ACL 问题，使 L3 可保存和切换不同 L1 地址。
35. 完成 L1 多来源 profile 与本地安装来源隔离设计，降低测试 L1、正式 L1、私有 L1 之间的包冲突风险。
36. 推进 Windows packaged 模式下 Skill/MCP 独立安装、订阅后显示控制台页面的能力。
37. 优化英语助手的 Skill/MCP/Model 拆分，补充本地词典、例句缓存、例句链路日志和质量检查思路。
38. 更新桌面端版本到 `v0.9.108`，解决 rebase 冲突，执行核心单测矩阵并推送 GitHub main 分支与版本 tag。

