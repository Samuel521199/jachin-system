# Simplified Dapr configuration (minimal setup for development)

apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: jachin-config
spec:
  tracing:
    samplingRate: "1"
  metric:
    enabled: true
  # Access control disabled for development (simpler)
  # accessControl:
  #   defaultAction: allow
