# 将下列键合并进 %USERPROFILE%\.jachin\mcp_servers.json 中 id 为 "browser-use" 的条目的 "env" 内。
# 使用绝对路径可避免 mcp_servers 里 "${BROWSER_USE_CONFIG_PATH}" 在 L3 进程未加载 .env 时展开为空。
# 合并后请重启 L3。（cdp_url 写在 browser-use-attach-cdp.json 里，不在此 env。）

  "BROWSER_USE_CONFIG_PATH": "C:\\Users\\Samuel\\.jachin\\runtime\\browser-use-attach-cdp.json"
