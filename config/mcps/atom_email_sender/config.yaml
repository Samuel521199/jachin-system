# atom_email_sender MCP — 不在此存放账号/密码/默认收件人
# BI 战报 Step 3.6 由 main_skill 从 bi_daily_report.yaml（distribution.email）与 .env（BI_SMTP_*、BI_SMTP_TO）读取；
# 此处仅保留占位；若 YAML/.env 均未配置，skill 才会尝试读本文件中的 smtp / default_to_addrs（可为空）。
smtp: {}
default_to_addrs: []
