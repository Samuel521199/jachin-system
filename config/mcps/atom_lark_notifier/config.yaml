# atom_lark_notifier — PMO-Copilot / Lark 播报（与应用机器人 IM API）
# 若 BI 战报仍需独立机器人：请将 BI 凭证放在 ~/.jachin/config/mcps/atom_lark_notifier/config.yaml（075 覆盖仓库）。
#
# 租户：国际版 Lark → open.larksuite.com（lark_use_feishu: false）
# 推送目标（二选一优先）：① default_webhook_url ② default_chat_id（应用机器人须在目标群内）
default_webhook_url: ""

robot_name: PMO-Copilot
app_id: cli_a940990299f8ded2
app_secret: UZogSbmne0IBiD81Ke0oieCNEmIdxNJD

# PMO 推送目标群 chat_id（可变；读 .env PMO_PRIMARY_CHAT_ID；须与本租户 OpenAPI 一致；机器人须已加入该群）
# 每次分支 A/B 播报需同时推送到 default_chat_id 与 monitoring_chat_id 两个会话。
default_chat_id: "${PMO_PRIMARY_CHAT_ID}"

# 固定监控群（后台存档/项目经理监控；不可变；不走环境变量）
monitoring_chat_id: "oc_0e321f92d758ecb44aea5b499c90510b"

lark_use_feishu: false

l3_ws_url: "ws://127.0.0.1:18981/sensory"

# 为 true 时：正文内 GFM | 表 | 转为飞书 Schema 2.0 原生 table 组件（PMO 战报推荐开启）
native_table_card: true
