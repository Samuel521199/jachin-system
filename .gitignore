# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
/downloads/
eggs/
.eggs/
/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
venv/
env/
ENV/
.venv
.venv-*/

# Conda
.conda/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
# .cursor/ 整目录纳入版本控制（含 rules 等），推送 GitHub 时不忽略

# Environment variables
.env
.env.txt
# FB Nexus 飞书密钥（与 scripts/fb_report_nexus.env.example 配套）
scripts/fb_report_nexus.env

.env.local
.env.*.local

# Lark/K11 内嵌键：由 scripts/emit_packaged_lark_env.py 从 .env 生成，含 secret，勿提交
l3_node/packaged_lark_env_generated.py

# Tauri updater signing key（勿提交私钥；公钥在 updater.key.pub / tauri.conf）
clients/desktop/src-tauri/updater.key

# Nexus pairing (memory mode fallback)
.nexus-pairing.json
.nexus-l2-bridge.json

# Logs
*.log
logs/
*.log.*

# Database
*.db
*.sqlite
*.sqlite3

# OS
.DS_Store
Thumbs.db
desktop.ini

# Docker
docker-compose.override.yml

# Secrets
secrets/
*.pem
*.key
*.crt
dapr/secrets/secrets.json

# Temporary files
tmp/
temp/
*.tmp
temp_invoke_body.json

# Node (for frontend)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Rust (for Tauri)
target/
Cargo.lock

# Build outputs
dist/
build/
*.exe
*.dmg
*.app

# Local generated outputs and large model artifacts
output/
data/models/
model/
models_repo/**/model/
*.gguf

# ChromaDB data
chroma_data/

# MQTT data
mqtt_data/
mqtt_logs/

# Jupyter Notebook
.ipynb_checkpoints

# PyCharm
.idea/

# VS Code
.vscode/

# Archived documentation (do not track in git)
docs/archive/

# 本地向量/大体积运行时数据（勿提交；若目录在仓库内可自行追加路径）

# Coverage
.coverage
htmlcov/

# TTS models (download via scripts/download_tts_models.py, exceed GitHub 100MB limit)
data/tts/*.onnx
data/tts/voices.json
data/tts/*.bin

# JVS voice models (SenseVoice STT + MOSS ONNX TTS; see data/models/voice/README)
data/models/voice/**/*.onnx
data/models/voice/**/*.bin

# HR analysis reports and resumes (user-specific runtime data)
data/hr_analysis/*.md
data/hr_resumes/*.pdf

# Local BI 提纯/output 与 volumes 镜像目录（勿提交）
bi_data/

# Build artifacts
skills_repo/*.zip
# L1 Nexus 本机 bulk-publish 生成的技能/MCP zip（体积大，可脚本重生成）
cloud/nexus/public/packages/*.zip

# L3 桌面包内嵌 Python 运行时（本地打包生成，体积 >100MB，勿提交）
dist_jachin_desktop/runtime/

# Temporary commit messages
commit_msg.txt
.git_commit_msg.txt
.git_tag_msg.txt
.git_commit_msg2.txt

# L1 Docker 部署本地密钥（从 docker/l1.env.example 复制）
docker/l1.env
# ECS 一键部署包内预填的 l1.env（含数据库密码）
deploy/l1-ecs-bundle/l1.env
deploy/l2-ecs-bundle/l2.env

# Local Docker image exports (large; do not commit)
/jachin-*-latest.tar
/jachin-*-latest.tar.gz
deploy/l1-docker-cloud/*.tar
deploy/l1-docker-cloud/*.tar.gz
deploy/l1-docker-cloud/l1.env

# FB 广告报告 CSV（脚本产出数据，不入仓库）
fb_ads_report_*.csv
fb_campaign_report_*.csv

# 调试截图（自动化测试过程中生成，不入仓库）
debug_*.png
scripts/parsed_output.jpg
scripts/parsed_result.json
scripts/test_screen.jpg
target_codex_check/
dist_fb_ph/
dist_fb_ph.zip

# OmniParser 标注图输出（scripts/omnioutput/README.md 保留）
scripts/omnioutput/*
!scripts/omnioutput/README.md
!scripts/omnioutput/.gitkeep

# 便携包 zip 产物（构建产物，体积大）
dist_jachin_desktop.zip

# Lark/K11 冒烟密钥（含 app_secret，勿提交）
dist_jachin_desktop/k11_smoke_lark.env
