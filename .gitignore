# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
venv/
env/
ENV/
.venv

# Conda
.conda/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.cursor/*
!.cursor/rules/

# Environment variables
.env
.env.txt
.env.local
.env.*.local

# Nexus pairing (memory mode fallback)
.nexus-pairing.json
.nexus-l2-bridge.json

# Logs
*.log
logs/
*.log.*

# Database
*.db
*.sqlite
*.sqlite3

# OS
.DS_Store
Thumbs.db
desktop.ini

# Docker
docker-compose.override.yml

# Secrets
secrets/
*.pem
*.key
*.crt
dapr/secrets/secrets.json

# Temporary files
tmp/
temp/
*.tmp
temp_invoke_body.json

# Node (for frontend)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Rust (for Tauri)
target/
Cargo.lock

# Build outputs
dist/
build/
*.exe
*.dmg
*.app

# ChromaDB data
chroma_data/

# MQTT data
mqtt_data/
mqtt_logs/

# Jupyter Notebook
.ipynb_checkpoints

# PyCharm
.idea/

# VS Code
.vscode/

# Archived documentation (do not track in git)
docs/archive/

# 本地向量/大体积运行时数据（勿提交；若目录在仓库内可自行追加路径）

# Coverage
.coverage
htmlcov/

# TTS models (download via scripts/download_tts_models.py, exceed GitHub 100MB limit)
data/tts/*.onnx
data/tts/voices.json
data/tts/*.bin

# HR analysis reports and resumes (user-specific runtime data)
data/hr_analysis/*.md
data/hr_resumes/*.pdf

# Local BI 提纯/output 与 volumes 镜像目录（勿提交）
bi_data/

# Build artifacts
skills_repo/*.zip

# L3 桌面包内嵌 Python 运行时（本地打包生成，体积 >100MB，勿提交）
dist_jachin_desktop/runtime/

# Temporary commit messages
commit_msg.txt
.git_commit_msg.txt
.git_tag_msg.txt
.git_commit_msg2.txt

# L1 Docker 部署本地密钥（从 docker/l1.env.example 复制）
docker/l1.env
# ECS 一键部署包内预填的 l1.env（含数据库密码）
deploy/l1-ecs-bundle/l1.env
deploy/l2-ecs-bundle/l2.env

# PMO/BMO 技能配置（含 Lark app_secret）
config/skills/com.jachin.pmo.bmo/pmo_bmo.yaml

# Local Docker image exports (large; do not commit)
/jachin-*-latest.tar
/jachin-*-latest.tar.gz
