# Kalaroko web perf monitor MCP - optional extras for E2E / sidecar build.
# If you already installed core/requirements.txt, most pins below overlap; verify versions.
#
# Required for Playwright E2E (Chromium); run after pip install:
#   playwright install chromium

mcp>=1.0.0
filelock>=3.13.0
numpy>=1.24.0,<2
fastembed>=0.4.0
httpx>=0.25.0
python-dotenv>=1.0.0
playwright>=1.40.0
# Optional: BrowserContext stealth (execute_playwright_perf_test); skip if not installed
playwright-stealth>=1.0.0
