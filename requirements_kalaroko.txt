# Kalaroko Web 性能监控哨兵 — MCP 依赖片段（可 pip install -r 本文件）
# 若已安装主仓 core/requirements.txt，则下列多数已满足，仅需确认版本。
#
# 【必做】安装 Playwright 浏览器内核（否则 execute_playwright_perf_test 无法启动 Chromium）：
#   playwright install chromium

mcp>=1.0.0
httpx>=0.25.0
python-dotenv>=1.0.0
playwright>=1.40.0
# execute_playwright_perf_test：BrowserContext 级隐身（可选；未安装则跳过）
playwright-stealth>=1.0.0
