# 精灵图集资源

本目录需要包含以下文件才能正确显示精灵形象：

- `core-atlas.json` ✓
- `core-atlas-1.png` 至 `core-atlas-5.png` ✓（缺失时使用紫色占位符）
- `core-normal-atlas.json` ✓（已存在）
- `core-normal-atlas-1.png` 至 `core-normal-atlas-5.png`（可选，法线贴图）

**获取图集 PNG**：使用 TexturePacker 等工具从源精灵图导出，或从项目资产包中复制。
图集 JSON 已就绪，只需补充对应的 PNG 图片文件。
