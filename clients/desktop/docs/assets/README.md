# 陪伴态 UI 基准截图

验收图（图1 原样）应存于此目录，文件名：`companion_baseline.png`。

**图1 验收标准**（见 `COMPANION_UI_REGRESSION_ROOT_CAUSE_ANALYSIS.md` §8.3）：

- 纵向顺序：Orb → 状态字 →「语音输入」，无重叠
- Orb 外圈 glow 无顶/底平切线
- 按钮在窗口 client 区域内，点击可开始 PTT

修复陪伴 UI 的 PR 请附 Esc 后新截图，与本基准并排对比。
