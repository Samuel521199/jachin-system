/** Shared TTS defaults. Keep these aligned with scripts/trace_kokoro_tts_pipeline.py without CLI overrides. */
export const DEFAULT_KOKORO_TTS_VOICE = "zm_053";
export const DEFAULT_KOKORO_TTS_SPEED = 1.25;

