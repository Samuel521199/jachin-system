# Jachin Nexus Layer 2 - v5.0 极简依赖 (The Zero-Friction Edition)
# 仅保留契合轻量边缘计算的核心包，已废弃 Dapr/Ray/Qdrant/PostgreSQL

# HTTP
httpx>=0.25.0

# LLM (OpenAI-compatible, 本地或云端)
openai>=1.0.0

# CLI & 终端
rich>=13.0.0

# WASM 物理沙箱
wasmtime>=15.0.0

# 配置
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# 阿里云 Qwen (可选)
dashscope>=1.17.0

# 工具
pyyaml>=6.0.1
aiofiles>=23.2.1
