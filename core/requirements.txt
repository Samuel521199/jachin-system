# Jachin Nexus Layer 2 - v8.0 (The Singularity OS)
# Core packages for edge computing

# HTTP
httpx>=0.25.0
aiohttp>=3.9.0  # L3 skill HTTP API

# Redis (L2 cluster state, leader election)
redis>=5.0.0

# LLM (OpenAI-compatible, local or cloud)
openai>=1.0.0

# CLI
click>=8.0.0
rich>=13.0.0

# WASM sandbox
wasmtime>=15.0.0

# Config
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Alibaba Qwen (optional)
dashscope>=1.17.0

# Utils
pyyaml>=6.0.1
aiofiles>=23.2.1

# v8.0 Vector (Quantum Memory + Semantic Router)
lancedb>=0.3.0
# 混合检索：向量 + BM25 关键词（专有名词、精确匹配）
rank-bm25>=0.2.2
pyarrow>=14.0.0
sentence-transformers>=2.2.0

# v8.0 Voice (Jachin Voice - STT & TTS)
SpeechRecognition>=3.10.0
pyaudio>=0.2.14
edge-tts>=6.1.0
pygame>=2.5.0

# V2 Zero-trust key flow
cryptography>=42.0.0

# 进程/主机遥测（Commander、Skills/Orchestrator 路由等）
psutil>=5.9.0

# MCP (Model Context Protocol) Track A
mcp>=1.0.0

# PDF resume text extraction (L2 host, outside Wasm)
pymupdf>=1.24.0
pypdf>=4.0.0
# OCR fallback: rapidocr-onnxruntime, easyocr (pip only, no system deps)
# onnxruntime needs numpy<2 (NumPy 2.x ABI incompatible)
numpy<2
rapidocr-onnxruntime>=1.3.0
easyocr>=1.7.0
Pillow>=9.0.0
# Test PDF (scripts/create_test_resume_pdf.py)
fpdf2>=2.7.0

# 定时任务（招聘心脏起搏器）
apscheduler>=3.10.4

# V2 L2 Admin JWT
PyJWT>=2.8.0
bcrypt>=4.0.0

# v8.0 The Singularity OS
litellm>=1.0.0
websockets>=12.0
# 招聘插件 atom_post_job_boss 等需 CDP 连接 Chrome（无需 playwright install chromium）
playwright>=1.40.0
pvporcupine>=3.0.0
opentelemetry-api>=1.20.0
aiosqlite>=0.19.0
