# Jachin Nexus Layer 2 - v8.0 极简依赖 (The Singularity OS)
# 仅保留契合轻量边缘计算的核心包，已废弃 Dapr/Ray/Qdrant/PostgreSQL

# HTTP
httpx>=0.25.0

# LLM (OpenAI-compatible, 本地或云端)
openai>=1.0.0

# CLI & 终端
click>=8.0.0
rich>=13.0.0

# WASM 物理沙箱
wasmtime>=15.0.0

# 配置
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# 阿里云 Qwen (可选)
dashscope>=1.17.0

# 工具
pyyaml>=6.0.1
aiofiles>=23.2.1

# v8.0 向量检索 (Quantum Memory + Semantic Router)
lancedb>=0.3.0
pyarrow>=14.0.0
sentence-transformers>=2.2.0

# v8.0 语音感官 (Jachin Voice - STT & TTS)
SpeechRecognition>=3.10.0
pyaudio>=0.2.14
edge-tts>=6.1.0
pygame>=2.5.0

# V2 零信任密钥流转
cryptography>=42.0.0

# v8.0 The Singularity OS
litellm>=1.0.0
websockets>=12.0
pvporcupine>=3.0.0
opentelemetry-api>=1.20.0
aiosqlite>=0.19.0
