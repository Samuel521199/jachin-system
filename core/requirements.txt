# Core Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# LLM Providers
dashscope>=1.17.0  # Alibaba Qwen
openai>=1.0.0      # OpenAI SDK
httpx>=0.25.0     # Local LLM (OpenAI-compatible)
tiktoken>=0.5.0    # Token counting

# Voice Services
edge-tts>=6.1.0    # Microsoft Edge TTS
openai-whisper>=20231117  # OpenAI Whisper
# Note: Whisper requires ffmpeg, install with:
# Windows: choco install ffmpeg
# macOS: brew install ffmpeg
# Linux: sudo apt-get install ffmpeg

# Database
sqlalchemy>=2.0.23
alembic>=1.12.1
asyncpg>=0.29.0    # PostgreSQL async driver
psycopg2-binary>=2.9.9  # PostgreSQL sync driver (for migrations)

# Vector Database
qdrant-client>=1.7.0  # Qdrant vector database client
lancedb>=0.4.0       # LanceDB local embedded

# Redis
redis>=5.0.1
hiredis>=2.2.3

# MQTT
paho-mqtt>=1.6.1

# WebSocket
websockets>=12.0
aiohttp>=3.9.0      # Nexus Daemon Local Ingress API

# gRPC
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.1

# Cryptography (for mTLS)
cryptography>=41.0.0

# Dapr
dapr>=1.11.0  # Dapr Python SDK

# CLI (core/cli.py)
click>=8.1.0
rich>=13.0.0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
aiofiles>=23.2.1
httpx>=0.25.0

# Logging
structlog>=23.2.0

# Testing (optional)
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0

# Ray distributed
ray[default]>=2.8.0

# Docker client
docker>=6.1.0

# Wasm runtime
wasmtime>=15.0.0

# Config
pyyaml>=6.0.1

# Service discovery
zeroconf>=0.131.0

# JSON Schema
jsonschema>=4.20.0
