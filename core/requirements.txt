# Core Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# LLM Providers
dashscope>=1.17.0  # 阿里云 Qwen
openai>=1.0.0      # OpenAI SDK（Commander Function Calling，兼容 DeepSeek/本地 vLLM）
httpx>=0.25.0     # Local LLM (OpenAI-compatible)
tiktoken>=0.5.0    # Token 精确统计（Context 显示，可选）

# Voice Services
edge-tts>=6.1.0    # Microsoft Edge TTS (免费语音合成)
openai-whisper>=20231117  # OpenAI Whisper (语音识别，推荐)
# Note: Whisper requires ffmpeg, install with:
# Windows: choco install ffmpeg
# macOS: brew install ffmpeg
# Linux: sudo apt-get install ffmpeg

# Database
sqlalchemy>=2.0.23
alembic>=1.12.1
asyncpg>=0.29.0    # PostgreSQL async driver
psycopg2-binary>=2.9.9  # PostgreSQL sync driver (for migrations)

# Vector Database
qdrant-client>=1.7.0  # Qdrant vector database client
lancedb>=0.4.0       # LanceDB 本地嵌入式向量库（RAG 战役一、Edge L1 缓存）

# Redis
redis>=5.0.1
hiredis>=2.2.3

# MQTT
paho-mqtt>=1.6.1

# WebSocket
websockets>=12.0

# gRPC
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.1

# Cryptography (for mTLS)
cryptography>=41.0.0

# Dapr
dapr>=1.11.0  # Dapr Python SDK

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
aiofiles>=23.2.1
httpx>=0.25.0

# Logging
structlog>=23.2.0

# Testing (optional)
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0

# Ray 分布式计算
ray[default]>=2.8.0

# Docker 客户端（技能运行时）
docker>=6.1.0

# Wasm 运行时（可选）
# wasmtime>=15.0.0  # 如果需要Wasm支持，取消注释

# 配置管理
pyyaml>=6.0.1

# 服务发现
zeroconf>=0.131.0

# JSON Schema验证
jsonschema>=4.20.0
