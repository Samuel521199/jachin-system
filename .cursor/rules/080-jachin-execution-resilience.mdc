---
description: Jachin 全系统执行韧性契约 — Skill/任务重试、策略链、有界退出与部分成功（设计强制）
alwaysApply: true
---

# Jachin 执行韧性（全 Skill / 全任务）

**完整规范**：`docs/JACHIN_EXECUTION_RESILIENCE_CONTRACT.md`（错误分类、RunReport、ExecutionBrief、分层职责、检查清单）。

在修改 **Skill、MCP 工具、Wasm 宿主、调度器、Agent 工具路径、core 工作流** 时，必须遵守以下约束；与文档冲突时以文档为准。

---

## 1. 四条原则（必须）

1. **韧性**：单点失败（LLM/Wasm/网络/单文件）不默认拖死整条链路；已落盘/已消费的进度须可续跑或被下游利用。  
2. **策略链**：对同一步骤 **有限次重试** 后必须 **换策略**（降级批量→逐份、跳过坏项、换模型等），禁止同参无限重试。  
3. **有界退出**：重试/策略/LLM 轮次或软预算触顶后，产出 **ExecutionBrief**（尝试了哪些方法、失败类别、建议人工动作），再 **显式停止** 自动扩张，避免空转烧 Token。  
4. **部分成功**：批量中子项失败须 **记录**（stage + error_class + message），**其余继续**；主流程在业务可交付条件下 **跑完**，且文案区分「全部成功」与「部分成功 + 异常说明」。

---

## 2. 错误分类（实现须可归因）

至少区分：`transient` | `resource` | `per_item` | `config` | `permanent`。Wasm OOB/内存类归 `resource`；单 PDF 坏归 `per_item`。编排据此决定重试、降级或跳过。

---

## 3. Skill / 工具实现（代码层）

- 批量任务：**禁止** 仅因个别子项失败而让整个工具返回「全盘失败」且无明细。  
- **禁止** 静默 `except: pass` 导致上层误判成功。  
- 优先返回或可解析结构：**RunReport**（`status`、`ok_count`、`failed_items[]`、`degraded`、`fallback_used`）。  
- 有副作用操作尽量 **幂等** 或可检测重复。

---

## 4. 编排 / 调度（代码层）

- **禁止** 在已部分写入（NDJSON/磁盘）后 **`raise` 截断整次 job**，导致外层多轮续跑或工作流无法继续（安全关键场景除外，须注释说明）。  
- 同锁内多轮补缺口时须有 **无进展检测**（避免死循环）。  
- 策略切换、Brief 须打 **可检索日志**（如 `[StrategyShift]`、`[ExecutionBrief]`）。

---

## 5. Agent / LLM（代码层）

- 工具连续返回同类错误时：**注入**「禁止同参再调」或强制 **Brief** 路径。  
- 模型降级（如 LiteLLM fallback）**替代不了** 业务步骤无限重试；须与步骤级上限配合。

---

## 6. PR 自检（改相关代码时心里过一遍）

部分成功？重试有上限？失败后会换策略或出 Brief？用户文案是否诚实？
