---
description: V2 项目结构与 Key Components 宪法
globs: core/**/*.py, cloud/nexus/**/*.ts, clients/**/*.tsx, l3_node/**/*.py, docs/*.md
---
# V2 项目结构宪法

**规范**: docs/ARCHITECTURE.md | docs/architecture/JACHIN_HYBRID_AGENT_ARCHITECTURE.md（L3 执行主轴） | docs/FILE_STRUCTURE.md | **术语** Tools/MCP/Skills/Agent Tasks：`docs/Jachin 视角的「四大原语」终极架构规范.md`（**072-jachin-four-primitives.mdc**）

## V2 分层

| 层级 | 职责 |
|------|------|
| **Layer 1** | 平台：商城、IAM、manifest、licenses；与 L2/L3 无直接耦合 |
| **Layer 2** | 控制面：子账号、权限、API Key、记忆、数字仓库、MCP、L3 调度；**不代理推理** |
| **Layer 3** | 执行面：单主轴 ReAct（`run_agent`）+ MCP/Skill；网关/语义层/内联 Critic/经验 RAG；持密文 Key 直连 API，技能从 L2 拉取 |

## Key Components

| 名词 | 说明 |
|------|------|
| **API Key 零信任** | L2 用 L3 公钥加密下发，L3 私钥解密后直连 API |
| **数字仓库** | L2 inventory：L1 同步 + 侧载；/skills、/download 需 X-Sub-Account-Id |
| **MCP 执行模型** | L3 默认执行面；跨节点 **目标** TaskManager + Pull（`docs/ARCHITECTURE_L3_MCP_HOST_AND_L2_TASK_MANAGER.md`）；**现状** 兼容 HTTP 见 `docs/MCP_EXECUTION_MODEL.md`；**stdio 的 `npx` 包名**须与 npm 一致，见 `docs/MCP_SPEC.md` §3.5、规则 **088-mcp-npm-package-verification** |
| **PolicyEnforcer** | RBAC 鉴权，断网时从 role_permissions 降级 |
| **Dream Weaver** | LanceDB 记忆聚类/去重/融合 |
| **Omni-Sensory Bus** | layer3_broadcast、能力协商 |
| **L3 智能化基线** | P0～P2 + **阶段 A～E**（锚点/计划卡/apply_patch/shell HITL/事件 JSONL/brainstorm·只读 verify·task_plan 门/§4.3 隐式学习+向量检测/`implicit_turn_attribution`）见总览 **§5.2～§六** 与规则 **078**、`docs/IMPLICIT_SIGNALS.md`；**长期三层编排**见 `docs/ORCHESTRATION_ARCHITECTURE.md`；对标文 **§九**（`JACHIN_VS_OPENCLAW_INTELLIGENCE_ANALYSIS.md`） |
| **L3 能力总目录** | `docs/L3_CAPABILITY_CATALOG.md` + `docs/capability_domains/` + `l3_node/capability_catalog.py`（`DOMAIN_REGISTRY`）；新 MCP/Skill 域须登记；Sidecar 打包见规则 **079**、`scripts/build_l3_sidecar.py` |
| **L3 轻量 + 订阅制品** | 本体仅核心+IM；Skill/MCP 经 L1→L2→缓存后即为 L3 运行时扩展；路径与打包约束见规则 **086**、`docs/L3_SLIM_DISTRIBUTION_AND_SUBSCRIBED_ARTIFACTS.md` |

## 禁止目录

- `core/dapr/`、`core/ray_cluster/`、`core/memory/schema/`（已移除）
