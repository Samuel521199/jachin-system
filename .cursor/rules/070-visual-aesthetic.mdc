---
description: Jachin 视觉灵魂 - 暗曜技术感 (Obsidian Tech) / HUD 风格，ROG 式卡片与实时数据
globs: clients/**/*.{tsx,ts,css,html} core/web_ui/**/*
---

# Visual Aesthetic Rules (Jachin HUD / Obsidian Tech)

## 核心定位
所有面向用户的 UI（桌面控制台、Web 管理后台、精灵 HUD）必须统一为 **「暗曜技术感」**：高对比度、霓虹点缀、控制台专业感与实时生命感。禁止静态死图与纯装饰性占位，数据必须可来自 Layer 1 API 实时刷新。

---

## 1. 主题色 (Theme)

| 用途           | 色值       | 说明                     |
|----------------|------------|--------------------------|
| 背景 (Background) | `#0D0D0D`  | 主背景，接近纯黑，利于 OLED |
| 警示 / 性能 (Alert, Performance) | `#FF0032` | 红，用于高负载、告警、性能曲线 |
| 指令 / 正常 (Command, Normal) | `#00F0FF` | 青，用于正常状态、指令、链接 |

- 次要点缀：紫色 `#A855F7`、银色 `#94a3b8` 可用于边框与辅助信息。
- 所有关键数据（CPU/GPU、延迟、节点状态）应使用上述主题色高亮，形成「仪表盘」式跃然感。

---

## 2. 组件风格 (Components)

- **仿 ROG Armoury Crate 卡片化**：信息以卡片为单位，非大块纯色。
- **圆角**：统一使用小角度圆角（如 `rounded-lg` / 8px），避免直角或过大圆角。
- **边缘**：每个卡片/面板具备 **1px 微光描边**，例如 `border border-white/10` 或 `box-shadow: inset 0 0 0 1px rgba(255,255,255,0.05)`。
- **玻璃感**：可选 `backdrop-blur-xl` + 半透明背景，与深空背景形成景深。
- **禁止**：大面积纯色块、无边框的扁平卡片、纯静态插图。

---

## 3. 字体 (Typography)

- **技术/数据/日志**：强制使用等宽字体，如 **JetBrains Mono** 或 **Fira Code**，增加专业感与可读性。
- **标题/品牌**：可使用 Orbitron 等 Sci-Fi 无衬线，与等宽形成层次。
- **正文**：Inter 或系统无衬线，保持可读性。

---

## 4. 实时性 (Real-time)

- **所有 UI 组件必须能够通过 Layer 1 API 或 HTTP 轮询实时刷新数据。**
- **禁止**：写死假数据、静态占位图、不可更新的「示例曲线」。
- 若后端暂未就绪，使用 **轮询或 WebSocket 占位**，并在代码中标注 `TODO: 接入 API`。
- 进度条、波形图、节点状态、延迟数字等必须有「跳动/更新」的视觉反馈，传达系统在「呼吸」和「思考」。

---

## 5. 功能映射（ROG → Jachin）

开发时按此对应关系设计控件与文案：

| ROG 界面元素     | Jachin 对应功能                | 意义 |
|------------------|--------------------------------|------|
| CPU/GPU 频率图   | Layer 2 算力状态               | 边缘节点推理压力 |
| 风扇转速/分贝    | Inference Latency（推理延迟）  | 大脑反应速度的「转速表」 |
| 运行模式切换     | Inference Strategy（推理策略） | 「极致精准」vs「敏捷反应」等一键切换 |
| 已连接设备       | JCP 节点地图                   | 手机、智能家居、Worker 在线状态 |

---

## 6. HUD / 精灵增强

- 桌面精灵 (Sprite) 与悬浮面板：**悬浮半透明**，背景毛玻璃或全透明，文字带轻微发光（如 `text-shadow` / 霓虹色）。
- **数据环绕**：精灵周围可环绕微小动态数值（如当前 Token 速度、本地 GPU 温度），由 Layer 2 或本地监控上报。

---

## 7. 适用范围

- **clients/desktop/**：Tauri 控制台、Dashboard、Neural Nexus、Skill Matrix、聊天窗口等。
  - 各模块的布局与组件设计以本规则约束视觉与实时性。
- **core/web_ui/**：Layer 2 Dashboard（节点管理、算力概览、技能市场预览、运行模式）。

修改以上路径下的 UI 时，请遵循本规则；新增页面时优先复用已有主题色与组件类（如 `glass-panel`、`neon-rose`、`neon-cyan`）。
