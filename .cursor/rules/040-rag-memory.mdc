---
description: v8.0 量子记忆 (Quantum Memory) — 生物学 + 向量 + 自我修复 + Dream Weaver 梦境重塑
globs: core/biological_memory.py, core/dreamer.py, core/agent_memory.py, core/vector_store.py, core/vector_router.py, core/embedding/
---
# Quantum Memory Pipeline (v8.0)

## 设计原则

- **该记住的死都不忘**：Core Memory 永久存入 `core_memory` 表，每次思考前注入 System Prompt。
- **该遗忘的随风而逝**：短期日志 24 小时后清理，梦境提纯时丢弃无用内容。
- **极简存储**：仅 SQLite (`~/.jachin/memory.db`) + sqlite-vss 或 lancedb 扩展。
- **自我修复**：工具报错时，错误日志作为 Observation 喂给大脑；梦境阶段可生成 bug_fix 规则。

---

## 记忆分层

| 层级 | 存储 | 用途 |
|------|------|------|
| **海马体** | `short_term_logs` | 24 小时内无损记录，供梦境消费 |
| **大脑皮层** | `core_memory` | 梦境提纯的高密度 Tag + 自我修复规则 |
| **向量检索** | sqlite-vss / lancedb | 百万级 Token 语义检索 |

---

## 实现要点

1. **短期写入**：`add_short_term(role, content, meta)` — Agent Loop 每次交互调用。
2. **向量写入**：`vector_store.embed_and_store()` — 可选，用于长对话检索。
3. **梦境引擎**：`core/dreamer.py` 凌晨 3 点（或可配置）触发，LLM 提纯 → 写入 `core_memory`。
4. **自我修复**：梦境阶段可生成 `bug_fix.md` 规则，写入 core_memory。
5. **Prompt 注入**：`get_core_memory_for_prompt(limit=20)` + 向量检索结果拼接到 System Prompt。
6. **清理**：`prune_short_term_older_than_24h()`、`delete_short_term_after_dream()`。
7. **v8.0 Dream Weaver Consolidation（梦境重塑与记忆自愈）**：设备空闲时，利用边缘轻量级 LLM 对 LanceDB 中的近期记忆进行聚类、去重、时间线衰减（忘却机制）和逻辑压缩。若发现记忆冲突，打上「需用户澄清」标签。详见 `docs/whitepaper/V8_SINGULARITY_OS.md`。

---

## 全域向量路由 (Semantic Router)

- **组件**: `core/vector_router.py` + `core/embedding/`
- **可插拔双引擎**（策略模式）:
  - **☁️ Cloud (OpenAIEmbedder)**: 调用 OpenAI/兼容 API，零本地负担。
  - **🛡️ Edge (ONNXEmbedder)**: 本地 sentence-transformers (all-MiniLM-L6-v2)，断网可用。
- **配置**: `~/.jachin/nexus_config.json` → `embedding_mode: "cloud"` | `"local"`，Layer 3 设置界面 "Local AI Mode" 开关。
- **机制**: `match_local_skill(intent: str)` 经可插拔 Embedder 转为向量，LanceDB `skills` 表余弦相似度检索，按相似度返回最匹配技能。
- **安全性（红线）**: 若本地未命中，向云端商城请求的“意念下载”**必须经过 Human-in-the-Loop (HITL) 强授权**（桌面弹窗或 IM 确认），**严禁静默下载执行未知云端逻辑**。

---

## 参考

- `docs/whitepaper/02_FRAMEWORK.md` 第三节（量子记忆）
- `docs/whitepaper/06_LAYER2_EDGE.md` 3.5 全域向量路由、3.6 Dream Weaver
- `docs/whitepaper/PLUGGABLE_VECTOR_ENGINE.md` 可插拔向量引擎设计
- `docs/whitepaper/V8_SINGULARITY_OS.md` v8.0 Dream Weaver Consolidation
