---
description: v8.0 量子记忆 (Quantum Memory) — 生物学 + 向量 + 自我修复 + Dream Weaver 梦境重塑
globs: core/biological_memory.py, core/dreamer.py, core/agent_memory.py, core/memory_store.py, core/db/l2_memory_lancedb.py, core/vector_router.py, core/embedding/, core/intelligence_implicit.py, core/intelligence_implicit_embedding.py, l3_client/local_mcps/jachin_memory_nexus/memory_backend.py, l3_node/memory_nexus_bridge.py, l3_node/local_memory_search.py, docs/MEMORY_SCORING.md, docs/architecture/MEMORY_NEXUS_L3.md, docs/IMPLICIT_SIGNALS.md
---
# Quantum Memory Pipeline (v8.0)

## 设计原则

- **该记住的死都不忘**：Core Memory 永久存入 `core_memory` 表，每次思考前注入 System Prompt。
- **该遗忘的随风而逝**：短期日志 24 小时后清理，梦境提纯时丢弃无用内容。
- **极简存储**：仅 SQLite (`~/.jachin/memory.db`) + sqlite-vss 或 lancedb 扩展。
- **自我修复**：工具报错时，错误日志作为 Observation 喂给大脑；梦境阶段可生成 bug_fix 规则。

---

## 记忆分层

| 层级 | 存储 | 用途 |
|------|------|------|
| **海马体** | `short_term_logs` | 24 小时内无损记录，供梦境消费 |
| **大脑皮层** | `core_memory` | 梦境提纯的高密度 Tag + 自我修复规则 |
| **向量检索** | sqlite-vss / lancedb | 百万级 Token 语义检索 |

---

## 实现要点

1. **短期写入**：`add_short_term(role, content, meta)` — Agent Loop 每次交互调用。
2. **向量写入**：L2 侧 `core/memory_store.py`（`add_memory_fragment` 等）、`core/db/l2_memory_lancedb.py` — 可选，用于长对话检索；**无**旧版独立 `vector_store` 模块。
3. **梦境引擎**：`core/dreamer.py` 默认凌晨 3 点 / 设备空闲触发；**另**：`dream_weaver.fragment_threshold`（`nexus_config.json`）未整合碎片达阈值 **立即** 触发 Dream Weaver，不必等凌晨 — 见 `docs/INTELLIGENCE_UPGRADE_OVERVIEW.md` §1.3、`core/dream_weaver.py`。
4. **自我修复**：梦境阶段可生成 `bug_fix.md` 规则，写入 core_memory。
5. **Prompt 注入**：`get_core_memory_for_prompt(limit=20)` + 向量检索结果拼接到 System Prompt。
6. **清理**：`prune_short_term_older_than_24h()`、`delete_short_term_after_dream()`。
7. **v8.0 Dream Weaver Consolidation（梦境重塑与记忆自愈）**：设备空闲时，利用边缘轻量级 LLM 对 LanceDB 中的近期记忆进行聚类、去重、时间线衰减（忘却机制）和逻辑压缩。若发现记忆冲突，打上「需用户澄清」标签。详见 `docs/whitepaper/V8_SINGULARITY_OS.md`。
8. **混合检索（Hybrid Search）**：**若部署 L2 控制面**，其 `GET /api/v2/memory/search` 支持向量 + BM25 + 可选 **MMR**；与 **L3 Memory Nexus**（`deep_search`）分立。权重与 MMR 见 `nexus_config.json` → **`memory_scoring`**，公式见 **`docs/MEMORY_SCORING.md`**。
9. **主动记忆刷新（Memory Flush）**：神盾 Compaction 前，当 token 接近上限时静默 LLM 回合，提醒模型写入 core_memory。配置 `llm.memory_flush.enabled`、`llm.memory_flush.soft_threshold`。
10. **Markdown 导出**：`export_core_memory_to_markdown()` 将 core_memory 导出至 `~/.jachin/memory/MEMORY.md`，梦境完成后自动执行；`GET /api/v3/memory/export-markdown` 手动触发。
11. **L3 Memory Nexus（现行）**：Chroma **`~/.jachin/palace_db`**，`l3_client/.../memory_backend.py`（`commit_drawer` / `recall_room` / `deep_search`）；L1 注入 · `core:local_memory_*` · 回合末异步 commit。可选 **`CHROMA_USE_HTTP_CLIENT`**。SSOT：**`docs/architecture/MEMORY_NEXUS_L3.md`**。
11b. **JSON「梦境合并」（已停用）**：`compact_local_memory_if_needed` 为 **no-op**；旧 `l3_local.json` LLM 合并调度已关闭。勿向用户灌输 150 条 banners 等旧口径；上下文 compaction 仍指 **`core/compaction_hook.py`**。
12. **Pre-reset 刷新（P0）**：`POST /api/v3/llm/context/reset` 前 `run_pre_reset_memory_flush()`（`core/compaction_hook.py`）。
13. **阶段 A（MVP）**：`llm.memory_flush.workspace_must_update` 锚点校验、`post_compaction_audit`、compaction 后 `findings` checkpoint（可选 **Nexus** `commit_drawer` 摘要，非旧 JSON 坍缩）；L3 与 core 共用 `compaction_before_llm_think`（`l3_node/l3_compaction_bridge.py`）。见总览 §5.2、**078**。
14. **P1 / P2（摘要）**：用户偏好与澄清队列、只读工具 TTL 缓存（`intelligence_p1.py`、`tool_call_cache.py`）；修正意图、意图-技能统计、检索强化分 + `POST /api/v2/memory/reinforce`（`intelligence_p2.py`）。**总表**：`docs/INTELLIGENCE_UPGRADE_OVERVIEW.md`。
15. **§4.3 隐式学习（越用越聪明）**：`core/intelligence_implicit.py`、`intelligence_implicit_embedding.py`；全端 **`implicit_turn_attribution`** + `POST /api/v2/intelligence/implicit-signal`；消费侧 **`intelligence_e`**。权威说明 **`docs/IMPLICIT_SIGNALS.md`**；与总览 **v1.9**、规则 **078** 一致。

---

## 全域向量路由 (Semantic Router)

- **组件**: `core/vector_router.py` + `core/embedding/`
- **可插拔双引擎**（策略模式）:
  - **☁️ Cloud (OpenAIEmbedder)**: 调用 OpenAI/兼容 API，零本地负担。
  - **🛡️ Edge (ONNXEmbedder)**: 本地 sentence-transformers (all-MiniLM-L6-v2)，断网可用。
- **配置**: `~/.jachin/nexus_config.json` → `embedding_mode: "cloud"` | `"local"`，Layer 3 设置界面 "Local AI Mode" 开关。
- **机制**: `match_local_skill(intent: str)` 经可插拔 Embedder 转为向量，LanceDB `skills` 表余弦相似度检索，按相似度返回最匹配技能。
- **编排 L1（长期架构）**: `l3_node/orchestration/skill_routing.py` 的 `suggest_skills_from_intent()` 封装上述能力，配置键 `nexus_config.orchestration`（`skill_routing_enabled`、`vector_router_threshold`）；与「上万 skill 只收窄候选、不一次性注入全量工具」策略一致 — 见 `docs/ORCHESTRATION_ARCHITECTURE.md`。
- **安全性（红线）**: 若本地未命中，向云端商城请求的“意念下载”**必须经过 Human-in-the-Loop (HITL) 强授权**（桌面弹窗或 IM 确认），**严禁静默下载执行未知云端逻辑**。

---

## 参考

- `docs/INTELLIGENCE_UPGRADE_OVERVIEW.md`（智能化落地与路线图，**v1.9**）
- `docs/IMPLICIT_SIGNALS.md`（§4.3 隐式信号与埋点）
- `docs/MEMORY_SCORING.md`（hybrid + reinforce + explain）
- `docs/ORCHESTRATION_ARCHITECTURE.md`（L1 技能路由与编排分层）
- `docs/whitepaper/02_FRAMEWORK.md` 第三节（量子记忆）
- `docs/whitepaper/06_LAYER2_EDGE.md` 3.5 全域向量路由、3.6 Dream Weaver
- `docs/whitepaper/PLUGGABLE_VECTOR_ENGINE.md` 可插拔向量引擎设计
- `docs/whitepaper/V8_SINGULARITY_OS.md` v8.0 Dream Weaver Consolidation
