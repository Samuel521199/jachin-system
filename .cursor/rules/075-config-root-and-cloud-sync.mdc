---
description: 配置根 ~/.jachin/ 与云端配置随包下发规范
globs: config/**/*, l3_node/**/*.py, core/sync_daemon.py, core/inventory_scanner.py, l3_node/mcp_sync.py
alwaysApply: false
---

# 配置根与云端配置随包下发规范

## 一、配置根：~/.jachin/

**所有配置必须写入 `~/.jachin/`**，禁止依赖项目根 `__PROJECT_ROOT__` 或 `get_app_root()` 作为配置路径。

### 目录结构

**原则**：相关配置必须归入对应技能或 MCP 的文件夹下，禁止在 config 根目录散落独立配置文件。

```
~/.jachin/
├── config/                    # 统一配置根
│   ├── skills/               # 按技能隔离，技能配置在其自身目录下
│   │   ├── com.jachin.bi.daily_report/
│   │   │   ├── bi_daily_report.yaml
│   │   │   └── bi_metrics.yaml
│   │   └── com.jachin.hr.analyzer4/
│   │       └── hr_jds/       # 该技能用的 JD 模板
│   │           └── *.md
│   └── mcps/                 # 按 MCP 隔离，MCP 配置在其自身目录下
│       ├── atom_web_scraper/
│       │   └── config.json
│       └── atom_bi_metrics/
│           └── bi_metrics.yaml
├── data/                     # 运行时数据
├── inventory/                # L2 同步的包
├── l3_mcp_cache/            # L3 拉取的 MCP
└── l3_skill_cache/           # L3 拉取的技能
```

### 实现要求

- 新增配置读取逻辑：优先 `Path.home() / ".jachin" / "config" / ...`，支持 `JACHIN_HOME` 环境变量覆盖
- 路径解析：`__PROJECT_ROOT__` 占位符替换为 `~/.jachin`，不再使用项目根

---

## 二、Skill / MCP 上传：必须包含配置

**发布到云端（L1）的 Skill 和 MCP 包必须包含其运行所需的配置模板。**

### 包结构（JMP/JSP）

```
{plugin_id}.jsp (ZIP)
├── manifest.json
├── signature.sig
└── payload/
    ├── main.wasm / tools/
    ├── plugin.json
    └── config/              # 必需：配置模板
        ├── manifest.yaml    # 配置写出清单
        └── ...              # 实际配置文件
```

### config/manifest.yaml 约定

写出路径必须落在 `config/skills/{skill_id}/` 或 `config/mcps/{mcp_id}/` 下，与包所属技能/MCP 对应。

```yaml
version: "1"
output_root: "~/.jachin"

writes:
  - path: "config/skills/com.jachin.hr.analyzer4/hr_jds"
    type: "directory"
    merge: "copy_missing"
  - path: "config/skills/com.jachin.bi.daily_report/bi_metrics.yaml"
    type: "file"
    merge: "overwrite_if_missing"
  - path: "config/skills/com.jachin.bi.daily_report/bi_daily_report.yaml"
    type: "file"
    merge: "overwrite_if_missing"
```

### 合并策略

| 策略 | 含义 |
|------|------|
| `copy_missing` | 仅当目标文件不存在时写入 |
| `overwrite_if_missing` | 目标不存在才写 |
| `never_overwrite` | 已有则跳过 |

### 敏感信息

- 配置模板使用占位符：`${BI_LARK_WEBHOOK_URL}`、`${BI_SMTP_PASSWORD}` 等
- 不将真实密钥写入包内

---

## 三、下载时自动写出配置

**L2 SyncDaemon 和 L3 同步逻辑在解压包后，必须按 `config/manifest.yaml` 将配置写出到 `~/.jachin/config/`。**

### L2 流程

1. 解压到 `~/.jachin/inventory/skills/{id}/` 或 `inventory/mcps/{id}/`
2. 若存在 `payload/config/manifest.yaml`，解析并执行写出
3. 按 merge 策略写入 `~/.jachin/config/`，不覆盖用户已修改

### L3 流程

1. 从 L2 拉取到 `~/.jachin/l3_mcp_cache/` 或 `l3_skill_cache/`
2. 若包内含 `config/`，执行相同写出逻辑
3. 保证 L3 单机部署时，仅靠下载即可获得完整配置

---

## 四、L3 单机部署（无 L1/L2）

**目标**：目标机只部署最小 L3，通过云端下载技能即可获得技能 + 配置，立即可用。

### 部署形态

```
目标机
├── l3_node.exe
├── .env（可选：API Key）
└── 无 config/、无 skills_repo/
```

### 用户操作

1. 安装 L3
2. 配对 L2（或通过 L2 代理连接 L1）
3. 同步技能 / MCP
4. 包内 config 自动写出 → 技能可用

### 实现要点

- L3 同步逻辑必须支持「包内含 config 时自动写出」
- 即使 L2 未执行写出，L3 拉取后也应自行写出，保证单机可用

---

## 五、禁止与检查清单

### 禁止

- 配置路径硬编码项目根（`__PROJECT_ROOT__`、`get_app_root()/config/`）
- 在 `config/` 根目录散落独立配置文件（如 `config/bi_metrics.yaml`），必须归入 `config/skills/{id}/` 或 `config/mcps/{id}/`
- 发布不含 config 的 Skill/MCP 包（若该技能依赖配置）
- 下载后不执行配置写出

### 检查清单（开发时）

- [ ] 新配置写入 `~/.jachin/config/skills/{skill_id}/` 或 `config/mcps/{mcp_id}/`，不散落于 config 根目录
- [ ] 打包时包含 `payload/config/` 及 `manifest.yaml`，writes 路径与包 id 对应
- [ ] SyncDaemon / mcp_sync 解压后执行配置写出
- [ ] 敏感项使用占位符
