---
description: V2 架构 — L3 执行面与 OpenClaw 对标（差异化见对标文档），L2 控制面不代理推理
globs: l3_node/**/*.py, core/db/**, core/api/routes/v2_*.py, core/security/**
---
# Jachin Nexus V2 — L3 单体架构

## 基准文档

[docs/ARCHITECTURE_V2_LAYER3_STANDALONE.md](../../docs/ARCHITECTURE_V2_LAYER3_STANDALONE.md)  
工具池与 MCP 合并流水线（与 Claude 风格对照、阶段 C 锚点）：[docs/architecture/L3_TOOL_POOL_AND_MCP_ASSEMBLY.md](../../docs/architecture/L3_TOOL_POOL_AND_MCP_ASSEMBLY.md)

## 术语（Tools / MCP / Skills / Agent Tasks）

**SSOT**：`docs/Jachin 视角的「四大原语」终极架构规范.md`（与 **072**、**045-four-primitives-execution.mdc** 同步）。讨论 `load_tools`、`mcp_registry`、`delegate`、`submit_background_task` 时统一使用 **Tools / MCP / Skills / Agent Tasks**。

## 核心原则

1. **L2 不代理推理**：L2 管理 API Key（密文下发）、权限、L3 协同调度等控制面；**L3 宿主跨会话记忆在本地 Memory Nexus（Chroma）闭环**，不依赖 L2 `/memory/*` 作为默认路径
2. **L3 直连 LLM**：L3 持密文 Key，请求时解密后自行调用 api.openai.com 等
3. **L3 执行面**：多 Agent、多 Skill、本地记忆、ReAct、YAML 工作流与 **领域子图委托**（`l3_node/orchestration/`、`core:domain_workflow_run`）；**前台超时 / 后台任务队列 / 工具后预取 / 规划链** 见 **`docs/前台闲聊与后台重负荷任务的物理隔离与背压熔断.md`**；与 OpenClaw **对标**见 `docs/JACHIN_VS_OPENCLAW_INTELLIGENCE_ANALYSIS.md`、`docs/INTELLIGENCE_UPGRADE_OVERVIEW.md` §五、**`docs/ORCHESTRATION_ARCHITECTURE.md`**

## L2 API (V2)

- `POST /api/v2/auth/sync` — L3 注册（公钥）
- `GET /api/v2/auth/poll?node_id=xxx` — L3 轮询审批状态（pending/approved + encrypted_api_keys）
- `GET /api/v2/keys` — L3 拉取密文 Key（需 sub_account_id）
- `POST /api/v2/admin/nodes/assign` — 管理员将节点分配给子账号
- `POST /api/v2/memory/sync` — 可选（多租户/集中式部署）；**本仓库 L3 默认不向 L2 同步宿主记忆**
- `POST /api/v2/admin/*` — 子账号、Key 管理

## L3 目录 (l3_node/)

- `llm_client.py` — SecurityContext + LiteLLMEngine
- `agent_core.py` — ReAct Agent（跨会话记忆：Memory Nexus，无 L2 记忆同步守护）
- `background_task_service.py`、`l3_event_bus.py`、`context_prefetch.py`、`foreground_tool_policy.py` — 见 `docs/前台闲聊与后台重负荷任务的物理隔离与背压熔断.md`
- `bootstrap.py` — 注册、拉 Key、创建引擎
- `capability_catalog.py` — 能力总目录注入（`DOMAIN_REGISTRY`）；新 MCP/域见规则 **079**

## 工具池（L3）

- **合并顺序**：`load_tools`（Native + jpp）在前，MCP（`fetch_tools_from_l2`）**追加**在后；权限上 MCP 与内置在 `allowed` 非空时统一走 `is_tool_allowed`。实现单点：`l3_node/primitives/tools/tool_pool.py` 的 `assemble_tool_pool`（`run_agent` await 调用）。
- **单点原则**：新增通道级工具过滤时优先改 `assemble_tool_pool`（或在其内分支），避免多处复制合并逻辑。
- **韧性**：MCP 拉取失败不得拖死整次 ReAct；须可降级为仅内置池。

## 禁止

- L2 代理 L3 的推理请求
- L3 明文存储 API Key（仅密文 + 内存解密）
