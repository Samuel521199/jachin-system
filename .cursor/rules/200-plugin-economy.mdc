---
description: v8.0 插件生态 — 四大原语（MCP / Skills / Tools / Agent Tasks）、JPP、商城
globs: core/system/**, core/wasm_runner.py, core/mcp_client.py, core/runtime/skill_loader.py, jachin-plugin-sdk-python/**, jachin-plugin-sdk/**
---
# Jachin 插件生态规则（v8.0 · 四大原语）

**SSOT**：`docs/Jachin 视角的「四大原语」终极架构规范.md`、**072-jachin-four-primitives.mdc**、本仓 **045-four-primitives-execution.mdc**。

## 1. 四类能力（工程侧）

### MCP（原语）
- **格式:** 连接 MCP 服务器，发现并注册 `mcp:*` 工具。
- **执行:** **L3 默认** `l3_node/primitives/mcp/registry.py`；**L2 侧载** `core/mcp_client.py`。跨节点见 `docs/ARCHITECTURE_L3_MCP_HOST_AND_L2_TASK_MANAGER.md`。
- **参考:** `docs/MCP_SPEC.md`、`docs/MCP_EXECUTION_MODEL.md`

### Skills（原语）
- **格式:** `skills_repo/**/SKILL.md`，YAML Frontmatter + 自然语言指令。
- **执行:** `core/runtime/skill_loader.py` 热加载，按需注入 Prompt。
- **参考:** `docs/SKILL_MD_SPEC.md`

### Tools — jpp / Wasm（原语）
- **格式:** 编译为 `.wasm`，`plugin.json` 声明。
- **Required:** `plugin.json` — 元数据、版税、Schema；`.wasm` — Rust / py2wasm 等。
- **执行:** `core/wasm_runner.py` / `l3_node/primitives/tools/loader.py` 注册为 `jpp:*`。
- **参考:** `docs/whitepaper/08_JPP_SDK_AND_SKILLS.md`

### Agent Tasks（原语）
- **形态:** `delegate`、后台 `core:submit_background_task`、`coordinate`；多轮、独立预算。
- **参考:** `docs/L3_AGENT_CONTEXT_MEMORY_AND_PROMPT.md`

---

## 2. JPP Manifest（Wasm / jpp Tool）

### Required Fields
- `id`, `version`, `name`, `description`
- `royalty_fee`: 版税分润
- `entry_point`: `plugin.wasm`
- `schema`: input/output

### Sandboxing（Tools · jpp）
- **WASM 沙箱:** `core/wasm_runner.py`，燃料熔断。
- **无 Ray:** v8.0 已废弃 Ray Actor。

---

## 3. 路由规则

Agent 在 ReAct `[Action]` 阶段（L3 为主）：
1. `action_name` 在 **MCP** 列表 → `mcp_registry` / `mcp_client`
2. 命中 **Skills**（SKILL.md 声明）→ 读 SKILL，关联 MCP / Tools
3. 否则 → **Tools**（`jpp` / `core:*` 经 `run_tool`）

---

## 4. 安全约束

- **MCP:** 高信任场景配置 `mcp_enabled`、`mcp_tool_allowlist`。
- **Skills:** 用户可控，`skills_repo/` 本地 Markdown。
- **Tools · jpp:** 零信任沙箱，与宿主机隔离。
