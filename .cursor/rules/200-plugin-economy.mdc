---
description: v8.0 三轨道技能体系、JPP、MCP、SKILL.md
globs: core/system/**, core/wasm_runner.py, core/mcp_client.py, core/runtime/skill_loader.py, jachin-plugin-sdk-python/**, jachin-plugin-sdk/**
---
# Jachin Skill Ecosystem Rules (v8.0)

## 1. 三轨道技能体系

### 轨道 A: MCP (Model Context Protocol)
- **格式:** 连接 MCP 服务器，发现并注册工具。
- **执行:** **L3 默认** `l3_node/skills/mcp_registry.py`；**L2 侧载** `core/mcp_client.py`。跨节点 **目标** `docs/ARCHITECTURE_L3_MCP_HOST_AND_L2_TASK_MANAGER.md`（Pull + Task Token）。
- **适用:** 高信任本地环境；L2 侧载为兼容网关场景。
- **参考:** `docs/MCP_SPEC.md`、`docs/MCP_EXECUTION_MODEL.md`

### 轨道 B: SKILL.md 声明式技能
- **格式:** `skills_repo/**/SKILL.md`，YAML Frontmatter + 自然语言指令。
- **执行:** `core/runtime/skill_loader.py` 热加载，按需注入 Prompt。
- **适用:** 用户可控，零编译。
- **参考:** `docs/SKILL_MD_SPEC.md`

### 轨道 C: JPP (Jachin Plugin Protocol)
- **格式:** 编译为 `.wasm`，配合 `plugin.json` 声明。
- **Required:** `plugin.json` — 元数据、版税、Schema；`.wasm` — 由 py2wasm 或 Rust 编译。
- **执行:** `core/wasm_runner.py` 通过 wasmtime 运行，支持 stdin/stdout JSON 协议。
- **适用:** 商城第三方付费插件，零信任沙箱。
- **参考:** `docs/whitepaper/08_JPP_SDK_AND_SKILLS.md`

---

## 2. JPP Manifest Specification (轨道 C)

### Required Fields
- `id`, `version`, `name`, `description`
- `royalty_fee`: 版税分润
- `entry_point`: `plugin.wasm`
- `schema`: input/output

### Sandboxing (轨道 C)
- **WASM 沙箱:** 每个插件在 `core/wasm_runner.py` 中独立执行，燃料熔断防死循环。
- **无 Ray:** v8.0 已废弃 Ray Actor，插件为 .wasm 二进制。

---

## 3. 路由规则

Agent 在 ReAct 的 `[Action]` 阶段（L3 桌面为主）：
1. 若 `action_name` 在 MCP 工具列表中 → **L3** `mcp_registry`（或 L2 编排链路中的 `core/mcp_client.py`）
2. 若 `action_name` 对应 SKILL.md 中声明的技能 → 读取 SKILL.md，关联 MCP 工具执行
3. 否则 → 走 `core/wasm_runner.py`（轨道 C）

---

## 4. 安全约束

- **轨道 A:** 仅高信任环境启用，可配置 `mcp_enabled`、`mcp_tool_allowlist`。
- **轨道 B:** 用户可控，SKILL.md 位于本地 `skills_repo/`。
- **轨道 C:** 零信任，WASM 物理隔离，无法访问宿主机。
