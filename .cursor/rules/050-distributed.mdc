---
description: v8.0 Layer 2 通信、Jachin Mesh、四大原语执行面、cron_thinker、持久化感官总线、全链路 runId、流式神经
globs: core/transport/**, core/system/**, core/daemon.py, core/agent_loop.py, core/cron_thinker.py
---
# Layer 2 Communication Rules (V2)

## 通信模型

- **Jachin Mesh:** 基于 WebSocket 的双向长连通道，实现 Layer 1 到 Layer 2 的**毫秒级指令下发**。替代 10 秒 HTTP 轮询。
- **生物钟:** `core/cron_thinker.py` 每 30 分钟主动环顾，读取 HEARTBEAT.md，异常时通过 IM 推送。
- **结果回传:** 执行完成后 POST `/api/v1/agents/result` 将结果推回用户（TG/飞书等）。
- **持久化感官总线:** OmniSensoryBus 底层挂载 SQLite 队列，确保进程重启不丢事件。
- **无 Dapr/Ray:** 已废弃。V2：**L3 单体执行**（Agent + Skill + 直连 LLM），**L2 控制面**（子账号、权限、记忆、L3 协同 / **目标** TaskManager + Pull）不代理推理。跨节点规格：`docs/ARCHITECTURE_L3_MCP_HOST_AND_L2_TASK_MANAGER.md`。

---

## 四大原语插件调用（L2 / L3）

术语 SSOT：`docs/Jachin 视角的「四大原语」终极架构规范.md`、**072**、**045-four-primitives-execution.mdc**。

### MCP
- **L2 侧载:** `core/mcp_client.py`（stdio MCP）。
- **L3 本机:** `l3_node/primitives/mcp/registry.py`；跨节点见 `docs/ARCHITECTURE_L3_MCP_HOST_AND_L2_TASK_MANAGER.md`。

### Skills（SKILL.md）
- **入口:** `core/runtime/skill_loader.py` — 热加载 `skills_repo/**/SKILL.md`。

### Tools（jpp / Wasm）
- **入口:** `core/wasm_runner.py`；协议见 `docs/whitepaper/08_JPP_SDK_AND_SKILLS.md`；沙箱 wasmtime + 燃料熔断。

### Agent Tasks
- 多轮 / 后台 / L2 编排：`l3_node/agent_core.py`（非单次 MCP 调用同一层级）。

---

## Envelope Pattern (可选，gRPC 场景)

若使用 Jachin Link (gRPC)：
- `InvokePlugin(PluginRequest) -> PluginResponse`
- 路由基于 `plugin_id`，payload 为业务数据。
- v8.0 主链路为 Jachin Mesh (WebSocket) + **四大原语执行面**，gRPC 为可选扩展。
