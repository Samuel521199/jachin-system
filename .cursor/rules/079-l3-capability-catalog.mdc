---
description: L3 能力总目录 — 新 MCP/Skill 必须登记；打包侧车须随 exe 分发 md
globs: l3_node/capability_catalog.py, l3_node/agent_core.py, docs/L3_CAPABILITY_CATALOG.md, docs/capability_domains/**/*.md, scripts/build_l3_sidecar.py, l3_node/skills/mcp_registry.py, skills_repo/**/plugin.json
alwaysApply: false
---

# L3 能力总目录（Capability Catalog）

## 单一事实来源

| 资源 | 路径 |
|------|------|
| **总目录（与业务域解耦）** | `docs/L3_CAPABILITY_CATALOG.md` — 系统身份、软/硬路由、域注册步骤、`PROMPT_INJECT_CORE` |
| **域切片** | `docs/capability_domains/<domain_id>.md` — 工具映射、硬路径说明、对应 `PROMPT_INJECT_*` |
| **运行时注册表** | `l3_node/capability_catalog.py` 的 `DOMAIN_REGISTRY` |

Agent 通过 `build_capability_prompt_inject_for_tools()` 将 **CORE + 命中域** 注入 system prompt，使大模型**自检「当前是谁、可见哪些能力」**。实现见 `agent_core._build_system_prompt`。

---

## 新增 MCP 或 Skill 时（必须做）

1. **若属于新「能力域」**（一批相关工具、需模型自检）  
   - 新建 `docs/capability_domains/<domain_id>.md`（含 `PROMPT_INJECT_<ANCHOR>_START/END`）。  
   - 在 `DOMAIN_REGISTRY` 追加 `CapabilityDomainSpec`（`tool_markers`、`doc_relpath`、`inject_anchor` 与 md 锚点一致）。  
   - 在 `docs/L3_CAPABILITY_CATALOG.md` **§4 已登记域索引** 表增一行。

2. **若仅扩展现有域工具**  
   - 更新该域的 `capability_domains/*.md` 中的工具表；若 markers 变化，同步改 `DOMAIN_REGISTRY.tool_markers`。

3. **L3 内置 MCP 列表**（如 `l3_node/skills/mcp_registry.py` 本地工具）变更时  
   - 若该工具属于某域，更新对应域 md；必要时为新域建文件并注册。

4. **不要**把具体业务 SOP 写进总目录正文；流程仍放在各包 `SKILL.md`（见 `docs/SKILL_MD_SPEC.md`）。

---

## 打包 L3 Sidecar（必须随二进制可用）

- 脚本：`scripts/build_l3_sidecar.py`  
- 已用 PyInstaller `--add-data` 将下列路径打入 `sys._MEIPASS/docs/`，与 `l3_node/capability_catalog._docs_dirs()` 的 frozen 分支一致：  
  - `docs/L3_CAPABILITY_CATALOG.md` → `docs/`  
  - `docs/capability_domains/` → `docs/capability_domains/`  

**修改总目录或域 md 后**：须重新执行 `python scripts/build_l3_sidecar.py`（或 CI 等价步骤），否则 exe 内仍为旧文案。

**便携包**：除 exe 内嵌外，仍可通过 `JACHIN_APP_ROOT` 指向的目录下的 `docs/` 覆盖或补充（见 `_docs_dirs` 解析顺序）。

---

## 关联规则与文档

- `065-v2-layer3-standalone.mdc` — L3 执行面  
- `077-skill-mcp-dependency.mdc` — Skill/MCP 依赖  
- `docs/SKILL_MD_SPEC.md`、`docs/HR_RECRUITMENT.md`（招聘域细节）
