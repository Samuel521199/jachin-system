# BI 战报 MCP 工具依赖（可选）
# 开发者 A/B 本地开发时可 pip install -r l3_node/requirements-bi.txt
# 统帅在组装阶段会合并到 core/requirements.txt

# DuckDB（数据存储 + 提纯逻辑在 main_skill）
duckdb>=0.9.0

# 网页抓取（开发者 A - tool_web_scraper）
requests>=2.28.0
beautifulsoup4>=4.12.0

# 邮件与 HTTP 已有：smtplib 内置，httpx 在 core/requirements.txt

# 干系人表 xlsx → Markdown（scripts/xlsx_to_bi_project_md.py）
openpyxl>=3.1.0

# 可选：本地统计图 / 数据可视化脚本
matplotlib>=3.7.0
seaborn>=0.13.0
numpy>=1.24.0
